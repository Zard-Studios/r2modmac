

export function TitleBar() {
    return (
        <div
            data-tauri-drag-region
            className="h-8 w-full flex-shrink-0 bg-transparent fixed top-0 left-0 z-50"
        />
    );
}
